export enum Theme {
  coding = "coding",
  gaming = "gaming",
  projects = "projects",
  foods = "foods",
}

export enum Player {
  blue = "blue",
  orange = "orange",
}

export enum FieldSize {
  small = 16,
  medium = 24,
  large = 36,
}
